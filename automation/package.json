{
  "name": "scrum-2-epam-contact-tests",
  "private": true,
  "version": "1.0.0",
  "description": "Playwright tests for SCRUM-2 (EPAM.com contact info access)",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.46.0"
  }
}
